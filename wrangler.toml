name = "portfolio"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
VITE_APP_TITLE = "个人作品集"

# 静态部署阶段不要启用下面配置。
# 配置 KV 后，在 Cloudflare Pages 控制台创建一个 KV 命名空间，
# 将绑定变量名设置为 SITE_DATA。
#
# [[kv_namespaces]]
# binding = "SITE_DATA"
# id = "填入真实 KV Namespace ID"
